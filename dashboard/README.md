# Dashboard skeleton

The T-002 dashboard substrate is React 18 with strict TypeScript and Vite. It displays
`INITIALIZING`, never a misleading protected state, until the authenticated C7/C8
implementation is delivered.

```powershell
npm install
npm run typecheck
npm test
npm run build
```

Dependency installation is intentionally not performed by repository generation.



